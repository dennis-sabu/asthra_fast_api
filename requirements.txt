# Asthra ID Scanner — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Note: paddlepaddle and torch (CPU builds) are installed from PyPI.
#       GPU acceleration requires dedicated CUDA builds.

fastapi
uvicorn[standard]
python-multipart
Pillow
opencv-contrib-python
numpy

# EasyOCR Engine (Fast Path)
torch
torchvision
easyocr
scikit-image

# PaddleOCR Ecosystem (PaddleOCR-VL Fallback)
paddlepaddle
paddleocr
paddlex[ocr]

# HTTP Client (for test_api.py & benchmarking)
requests